# بوت الدعم الصوتي الذكي 🤖🎧

بوت Discord احترافي لنظام الدعم الصوتي مع ذكاء اصطناعي من DeepSeek

## ✨ المميزات

- 🎤 **نظام صوتي متقدم** - قنوات صوتية ذكية
- 🤖 **ذكاء اصطناعي** - 3 شخصيات مختلفة (سارة، نور، ياسمين)
- 📲 **إشعارات فوري** - تنبيهات للمشرفين
- 🎵 **موسيقى انتظار** - تشغيل صوت أثناء الانتظار
- 🔒 **نظام قفل الاشتراک** - للسيرفرات المتعددة
- 📊 **إحصائيات** - معلومات شاملة عن الاستخدام

## 🚀 التثبيت والتشغيل

### على Railway (الطريقة الموصى بها)

**لا تحتاج لملف .env - ضع المتغيرات مباشرة في Railway:**

1. ربط Repository في Railway
2. أضف المتغيرات في Railway Dashboard:
   - `DISCORD_TOKEN` = توكن البوت
   - `DEEPSEEK_API_KEY` = مفتاح API
3. Deploy! 🚀

### محلياً (للتطوير)

#### 1. نسخ المشروع
```bash
git clone <repo-url>
cd test-bot
```

#### 2. تثبيت الاعتماديات
```bash
npm install
```

#### 3. إعداد المتغيرات البيئية (اختياري للاختبار)
```bash
cp .env.example .env
# عدّل .env وأضف توكنك ومفتاحك
```

#### 4. التشغيل
```bash
npm start
```

## 📋 متطلبات النظام

- Node.js 16+
- npm أو yarn
- توكن Discord Bot صحيح
- مفتاح DeepSeek API

## 🎯 أوامر البوت

### أوامر المالك (المفتاح: `!`)
- `!panel` - عرض لوحة التحكم
- `!stats` - إحصائيات البوت
- `!servers [page]` - قائمة السيرفرات
- `!lock <server_id>` - قفل السيرفر
- `!unlock <server_id>` - فتح السيرفر
- `!leave <server_id>` - مغادرة السيرفر

### أوامر الإعداد (Slash Commands)
- `/setup category <id>` - تعيين التصنيف
- `/setup voice <id>` - تعيين روم الصوت
- `/setup text <id>` - تعيين روم الإشعارات
- `/setup role <id>` - تعيين رتبة الإدارة
- `/setup show` - عرض الإعدادات
- `/reset` - مسح الإعدادات

### أوامر AI
- `/ai setup <enable|disable>` - تفعيل/تعطيل AI
- `/ai test [message]` - اختبار AI
- `/ai personality <type>` - تغيير الشخصية
- `/ai status` - حالة AI
- `/automatic` - العودة للنظام العادي

## 🛠️ البناء والنشر على Railway

### الملفات المطلوبة
- ✅ `bot.js` - الملف الرئيسي
- ✅ `deepseek-ai.js` - نظام AI
- ✅ `package.json` - الاعتماديات
- ✅ `railway.json` - إعدادات Railway
- ✅ `.env.example` - قالب المتغيرات

### خطوات النشر على Railway

1. **اربط الـ Repository**
   ```bash
   git push origin main
   ```

2. **أضف المتغيرات البيئية في Railway**
   - `DISCORD_TOKEN`
   - `DEEPSEEK_API_KEY`

3. **أعد تشغيل الخدمة**
   - Railway سيشغل `npm install` و `npm start` تلقائياً

## ⚙️ الإعدادات المطلوبة

### في Discord Server
1. إنشء تصنيف للرومات الخاصة
2. إنشاء روم صوت للانتظار
3. إنشاء روم نصي للإشعارات
4. إنشاء رتبة للمشرفين

### في البوت
```
/setup category <category_id>
/setup voice <voice_channel_id>
/setup text <text_channel_id>
/setup role <admin_role_id>
```

## 📝 ملاحظات مهمة

- 🔐 **لا تشارك توكنك أو مفاتيحك** في العلني
- 🚀 **استخدم Railway أو Heroku** للنشر
- 📦 **تثبيت ffmpeg** قد يكون مطلوباً على بعض الأنظمة
- 🎤 **ملفات الصوت** يجب أن تكون في نفس المجلد

## 🐛 استكشاف الأخطاء

إذا واجهت مشاكل:
1. تحقق من توكن البوت
2. تحقق من مفتاح DeepSeek API
3. تأكد من الصلاحيات الصحيحة للبوت
4. شاهد السجلات في Terminal

## 📧 الدعم والمساعدة

للمساعدة والإبلاغ عن الأخطاء، يرجى فتح Issue في المشروع.

---

**آخر تحديث:** فبراير 2026 ✅
