# 🔧 دليل استكشاف الأخطاء والمشاكل الشائعة

## ❌ المشاكل الشائعة والحلول

### 1️⃣ خطأ "توكن غير صحيح"

**الرسالة:**
```
Error [TokenError]: An invalid token was provided.
```

**الحل:**
1. تأكد من نسخ التوكن بشكل صحيح من [Discord Developers](https://discord.com/developers/applications)
2. لا تضع مسافات قبل أو بعد التوكن
3. تأكد من أن المتغير `DISCORD_TOKEN` موجود في `.env`

```
DISCORD_TOKEN=your_token_without_spaces
```

---

### 2️⃣ خطأ "Node.js لم يجد module"

**الرسالة:**
```
Cannot find module 'discord.js'
```

**الحل:**
```bash
# أعد تثبيت الاعتماديات
npm install

# أو امسح node_modules وأعد التثبيت
rm -rf node_modules package-lock.json
npm install
```

---

### 3️⃣ بوت لا يرد على الأوامر

**الأسباب المحتملة:**

1. **Intents غير مفعلة:**
   - الكود يحتوي على الـ intents الصحيحة ✅

2. **بدون صلاحيات الأوامر:**
   ```
   تأكد من:
   - البوت له Send Messages
   - البوت له Embed Links
   - البوت له Connect + Speak (للصوت)
   ```

3. **Slash Commands لم تسجل:**
   - البوت يسجلها تلقائياً عند العودة الأولى
   - انتظر 1 دقيقة ثم جرب `/help`

---

### 4️⃣ خطأ "الاتصال الصوتي فشل"

**الرسالة:**
```
Failed to connect to voice channel
```

**الحل:**
1. افحص أن ffmpeg مثبت:
   ```bash
   which ffmpeg  # على Linux/Mac
   where ffmpeg  # على Windows
   ```

2. إذا لم يكن مثبت:
   ```bash
   # Linux
   sudo apt-get install ffmpeg
   
   # Mac
   brew install ffmpeg
   
   # Windows
   choco install ffmpeg  # إذا كان عندك Chocolatey
   ```

3. تأكد من صلاحيات البوت:
   - Connect (دخول الروم الصوتي)
   - Speak (الكلام)
   - Use Voice Activity

---

### 5️⃣ AI لا يرد على الرسائل

**الأسباب:**

1. **DEEPSEEK_API_KEY ناقص أو خاطئ:**
   ```
   افحص أن المتغير موجود في .env
   ```

2. **الاتصال بـ DeepSeek API فشل:**
   ```
   تحقق من اتصال الإنترنت
   جرب: curl https://api.deepseek.com/v1/models
   ```

3. **AI معطل في السيرفر:**
   ```
   استخدم: /ai status
   استخدم: /ai setup enable
   ```

---

### 6️⃣ البوت يستHanging أو لا يعمل

**الحل:**

في Railway:
1. اذهب إلى Logs
2. ابحث عن الأخطاء
3. أعد التشغيل من Dashboard

في Local:
```bash
# اضغط Ctrl + C
# ثم جرب من جديد
npm start
```

---

### 7️⃣ "لا توجد نتيجة البحث"

**عندما تحاول تشغيل الأوامر:**

```bash
# حاول مسح الأوامر المخزنة
# افصل البوت ثم اتصل به مرة أخرى

# أو استخدم:
/help  # لعرض قائمة الأوامر
```

---

## 🔍 كيفية التشخيص

### 1. افحص السجلات (Logs)

**في Railway:**
- اذهب إلى Deployments → Latest → View Logs

**في Local:**
```
الرسائل تظهر في Terminal مباشرة
```

### 2. رسائل التشخيص المفيدة

| الرسالة | المعنى | الإجراء |
|---------|--------|--------|
| `🚀 بدء تشغيل البوت` | البوت شغال | ✅ طبيعي |
| `❌ فشل تسجيل الدخول` | توكن خاطئ | ❌ أعد التوكن |
| `🤖 DeepSeek API: ✅ متصل` | AI يعمل | ✅ طبيعي |
| `❌ خطأ في voiceStateUpdate` | مشكلة صوت | ⚠️ افحص ffmpeg |

### 3. تفعيل Debug Mode (اختياري)

أضف في أول bot.js:
```javascript
process.env.DEBUG = 'discord.js:*';
```

---

## 📚 موارد مفيدة

| المورد | الرابط |
|--------|--------|
| Discord Developers | https://discord.com/developers |
| Discord.js Docs | https://discord.js.org |
| DeepSeek API | https://platform.deepseek.com |
| Railway Docs | https://docs.railway.app |
| Node.js Docs | https://nodejs.org/docs |

---

## 💡 نصائح مفيدة

1. **استخدم Logging:**
   ```javascript
   console.log('🟢 البيانات:', data);
   console.error('🔴 الخطأ:', error);
   ```

2. **اختبر محليات أولاً:**
   ```bash
   npm start
   # جرب الأوامر في Discord
   ```

3. **اقرأ رسائل الخطأ بعناية:**
   - غالباً تحتوي على معلومة الحل

4. **استخدم await بشكل صحيح:**
   - جميع العمليات الصوتية تحتاج await

---

## 🆘 إذا ظلت المشكلة

1. افحص جميع الخطوات أعلاه
2. تحقق من السجلات (Logs) بحثاً عن رسالة خطأ محددة
3. جرب بحث Google عن الرسالة بالضبط
4. اطلب مساعدة من مجتمع Discord.js

---

**آخر تحديث:** فبراير 2026
**هل لديك مشكلة؟** تحقق من القائمة أعلاه! ✅
